[pytest]
# Run from repo root: pytest mcp-server/tests -q
# Adds mcp-server/ to sys.path so tests can import config, server, modules.*
pythonpath = mcp-server
testpaths = mcp-server/tests knowledge/tests
asyncio_mode = auto
